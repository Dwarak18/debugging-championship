# Nixpacks config — full-repo deployment for Railway
# This ensures section directories are available alongside backend/

[phases.setup]
nixPkgs = ["nodejs_20", "git", "python311"]

[phases.install]
cmds = ["cd backend && npm install --omit=dev"]

[start]
cmd = "cd backend && node app.js"
