{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c 'from core.database import init_db; init_db()' && uvicorn app:app --host 0.0.0.0 --port $PORT --workers 2 --proxy-headers",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
